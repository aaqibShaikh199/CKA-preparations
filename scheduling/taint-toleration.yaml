#How to Taint a NODE
#kubectl taint node <NODE-NAMR> <KEY>=<VALUE>:<EFFECT>

#Taint-toleration file 
apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    env: test
spec:
  containers:
  - name: nginx
    image: nginx
  tolerations:
  - key: "KEY
    effect: "EFFECT"
    value: "VALUE"
